AUDE.md — 自生长知识库规则

> 本文件是知识库的「操作系统」。AI 在整理、编译、问答、体检时都**必须遵守**这里的规则。
> 方法论来源：Andrej Karpathy 的 LLM Wiki —— 原始资料由 AI **逐步编译**成互相链接的 Wiki；规模足够后直接问它，而不是每次从头检索。

---

## 一、目录结构（五大类）

| 目录 | 用途 | 谁写 |
|------|------|------|
| `raw/` | **原始资料**：别人的文章、网页剪藏、PDF、书摘、截图等外部参考。**不可变**，AI 只读不写 | 你导入 |
| `mine/` | **我自己写的东西**，分三类：<br>`未定稿/` 随笔、草稿<br>`已定稿/` 定稿成稿（可用于提炼你的写作风格）<br>`AI辅助生成/` AI 帮你写的稿子（**不算你的风格**） | 你 / AI 辅助 |
| `wiki/` | AI 把 raw 与 mine **编译**出的、**互相链接**的条目 + 主题索引页 + `index.md`（总索引）+ `log.md`（维护日志） | AI 写 |
| `output/` | **运行产物**：问答产出（`问答-*.md`）、体检报告（`体检报告-*.md`） | AI 写 |
| `skills/` | **沉淀的可复用技能/工作流**：某任务反复做、值得固化成可直接调用的流程 | AI 写，你审核 |

> ⚠️ 注意：`copilot/skills/` 是 Obsidian Copilot 插件的自带目录，**不是**本库的 skills，不要动、不要混淆。本库技能统一放根目录 `skills/`。

---

## 二、wiki 文章标准格式

每篇 wiki 文章统一如下（**frontmatter + 摘要 + 正文 + 相关 + 来源**）：

```markdown
---
title: 条目名
type: entry | index | synthesis | term
created: YYYY-MM-DD
updated: YYYY-MM-DD
tags: [主题, 子主题]
sources: [原始资料文件名或链接]
---

# 条目名

## 摘要
（2-4 句话，扫一眼就懂这条在讲什么）

## 正文
（定义、要点、示例、对比；用 [[双链]] 连接其它 wiki 条目）

## 相关
- [[相关条目A]]
- [[相关条目B]]

## 来源
- raw/xxx.pdf
- mine/已定稿/yyy.md
- 或外部链接
```

- `index.md` 是**总索引**（问答第一步先查它）；每个主题再有一个 `主题-index.md` 主题索引页。
- 术语/速查类用 `type: term`；跨条目串讲用 `type: synthesis`。

---

## 三、新开文章 vs 并入已有

- **并入已有**：新资料只是补充某个已存在条目的细节、更新数据、纠正表述 → 直接编辑该条目，更新 `updated` 与 `来源`，**不要新建**。
- **新开文章**：出现一个**尚无对应条目**的独立概念/实体/主题，且不能与现有条目简单合并时，才新建。
- 判断口诀：**能当「一段的补充」就并入；是「另一个东西」才新开。**

---

## 四、链接规则（双链）

- 用 Obsidian `[[条目名]]` 互链，**不带 `.md`**。
- **链接目标不存在 → 先建一个占位页**（只写 frontmatter + 一行摘要 + `## 相关` + `## 来源` 标注「待补」），再回填链接。**绝不留悬空链接长期存在**。
- 每篇条目 `## 相关` 至少 2 个链接，强化知识网络；定期消除孤儿页。

---

## 五、关于 mine 与写作风格

- 只有 `mine/已定稿/` 的内容可用于**提炼你的写作风格**（语气、结构、用词）。
- `mine/AI辅助生成/` 的稿子**不算你的风格**，只能当素材，不能据此模仿「你的口吻」。
- `mine/未定稿/` 视为半成品，提炼风格前需你确认是否定稿。

---

## 六、问答模式（回答问题时）

1. **先查 `wiki/index.md`**，定位相关主题与条目。
2. **再到 `wiki/` 读对应条目**，必要时读 `raw/` 原文核实。
3. **只用 wiki 里已有的资料作答，绝不编造**。资料不足时明确说「wiki 里暂无此内容」，并建议补充 raw。
4. **答案要体现来源**：文末列出引用的 `[[条目]]` 与 `sources` 文件。
5. 有价值的回答/对比/串讲，沉淀成 `output/问答-*.md` 或新的 wiki 条目。

---

## 七、什么时候把工作流沉淀进 skills/

满足以下多条时，把反复出现的工作流固化成 `skills/` 下一份可直接调用的流程文件：

- 同一类任务你已做了 **≥3 次**（或预期会反复做）；
- 步骤固定、可模板化（建结构、写某类文档、跑某类检查）；
- 值得「下次直接调用」而不用每次重新想。

文件格式：`skills/<名称>.md`，含：**适用场景、输入、步骤、注意事项、示例**。

---

## 八、每日任务（整理新资料进 wiki）

每次新增资料后执行：

1. 归类：外部参考 → `raw/`；自己的内容 → `mine/` 对应子类。
2. 读 raw / mine，提取要点。
3. 按「三、新开 vs 并入」决定新建或并入 wiki 条目；按标准格式写/改。
4. 补全 `[[双链]]`（缺失目标则建占位页）。
5. 更新 `wiki/index.md`（若有新主题）与 `wiki/log.md`（记一笔：日期 + 摄入了什么）。
6. 触发一次轻量链接检查。

---

## 九、每周任务（体检）

每周执行一次，产出 `output/体检报告-YYYY-MM-DD.md`：

1. **查孤儿页**：找出没有任何入链的 wiki 条目，补链接或合并。
2. **查断链**：扫描所有 `[[链接]]`，列出目标不存在的（除已规划占位）。
3. **提炼风格**：若有新的 `mine/已定稿/`，抽样提炼可复用的语气/结构要点，记入风格笔记。
4. **体检报告**：汇总上述结果 + 知识库规模（条目数、主题数、本周新增）+ 建议。
5. 视情况把本周反复出现的工作流提议沉淀进 `skills/`。

---

## 十、初始状态

- 首批条目来自「云原生」主题（19 篇，已并入 `wiki/`，主题索引见 [[云原生-index]]）。
- `raw/`、`mine/` 目前基本为空，等待你持续投放资料后由 AI 编译生长。
- 历史 seed 条目沿用上一版模板（含 `学习目标/核心概念/See Also/参考资料`），将逐步补齐「摘要」段以对齐新标准。
